// import React, { useContext } from 'react';
// import { FormattedMessage } from 'gatsby-plugin-intl';

// import { SearchContext } from 'utils/contexts';
// import { ErrorHeader, ErrorWrapper, Spinner } from 'elements';
// import { Grid } from './elements';
// import { Tile } from '.';

const SearchResults: React.FC = () => {
  // const { loading, nothingFound, searchResults } = useContext(SearchContext);

  // if (nothingFound) {
  //   return (
  //     <ErrorWrapper>
  //       <ErrorHeader>
  //         <FormattedMessage id="errors.nothingFound" />
  //       </ErrorHeader>
  //     </ErrorWrapper>
  //   );
  // }

  // if (loading) {
  //   return <Spinner />;
  // }

  // return (
  //   <Grid>
  //     {searchResults.map(props => (
  //       <Tile key={props.id} data={props} />
  //     ))}
  //   </Grid>
  // );

  return <div>SearchResults</div>;
};

export default SearchResults;
